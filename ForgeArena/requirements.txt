# ForgeArena — AMD ROCm dependencies
# vLLM + Torch are pre-installed in AMD Cloud image
# Install: pip install -r requirements.txt

# Core
numpy>=1.24
jsonlines>=4.0

# Web UI (optional)
flask>=3.0

# Model download (optional - pick one)
# modelscope>=1.38  # for Chinese mirror
# huggingface-hub>=0.25  # for HuggingFace

# Inference backend (choose one)
# vllm>=0.23       # AMD ROCm version
# torch>=2.10      # ROCm build
