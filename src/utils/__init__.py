"""Generic utilities: image I/O, file helpers, logging setup.

Keep this layer dependency-free of business logic. Anything that imports
from `src.core` or `src.models` belongs higher up the stack.
"""
