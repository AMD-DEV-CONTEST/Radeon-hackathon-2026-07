# =============================================================================
# AMD Radeon Studio - 依赖列表
# =============================================================================
# 注意：PyTorch 必须装 ROCm 版本，命令见 README，不要直接 pip install torch
# 推荐：pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.1
# =============================================================================

# ---- Diffusion & Transformers（核心）----
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.34.0
safetensors>=0.4.4
huggingface_hub>=0.25.0
sentencepiece>=0.2.0
tokenizers>=0.20.0
protobuf>=5.28.0

# ---- 图像/视频处理 ----
opencv-python>=4.10.0
pillow>=10.4.0
imageio[ffmpeg]>=2.35.0
av>=12.0.0
ffmpeg-python>=0.2.0

# ---- 量化 / 性能优化（可选）----
bitsandbytes>=0.44.0           # 8-bit / 4-bit 推理
optimum>=1.23.0                # ONNX / BetterTransformer 加速

# ---- Web / API ----
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gradio>=4.44.0                 # 快速搭 Web UI
pydantic>=2.9.0
python-multipart>=0.0.12
websockets>=13.0

# ---- ComfyUI 客户端（可选）----
websocket-client>=1.8.0
requests>=2.32.0

# ---- Agent / LLM 工具（可选，备用云端 API）----
openai>=1.50.0                 # OpenAI 兼容客户端，DeepSeek/Qwen 都支持

# ---- 音频（可选）----
soundfile>=0.12.1
librosa>=0.10.2
scipy>=1.14.0

# ---- 工具 ----
tqdm>=4.66.0
pyyaml>=6.0
python-dotenv>=1.0
rich>=13.9.0
